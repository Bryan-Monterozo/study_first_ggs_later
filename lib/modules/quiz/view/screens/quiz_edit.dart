import 'package:flutter/material.dart';

class QuizEdit extends StatelessWidget {
  const QuizEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}